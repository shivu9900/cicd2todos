# Example environment variables for todo-app

# For local development (MySQL running locally or via Docker)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=todos

# For Cloud Run deployment, DB_HOST is set to the Cloud SQL Unix socket path via --set-env-vars:
# DB_HOST=/cloudsql/YOUR_PROJECT_ID:us-central1:todo-mysql
